# Security

Do not open public issues containing write tokens, private artifact content, or server logs with sensitive data.

Report vulnerabilities privately through GitHub's **Security advisories** tab for this repository.

Artifacts may be visible across the configured LAN. Treat every publish operation as disclosure to every client able to reach that server.
